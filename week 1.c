/*
#include <stdio.h>

// exercise: print all powers of 2 less than 100
void powerOfTwo() {
    for (int number = 1; number < 100; number*=2) {
        printf("%d\n", number);
    }
}

// exercise: write a function printArray that prints the array in the output
void printArray(int elements[], int size){
    for (int arrayIndex = 0; arrayIndex < size; arrayIndex++) {
        printf("%d, ", elements[arrayIndex]);
    }
}

int main() {
    powerOfTwo();

    int elements[] = {-1, 10, 0, 4};
    int size = sizeof elements / sizeof elements[0];
    printArray(elements, size);

    return 0;
}
*/
