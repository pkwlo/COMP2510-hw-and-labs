/*
#include <stdio.h>

// exercise: write a function that increments an integer's value by 1
void incrementByOne(int *number) {
    printf("%d\n", ++*number);
}

// exercise: write a function that calculates the sum of the values in an integer array
int calculateSum(int num[], int size) {
    int sum = 0;
    for (int index = 0; index < size; index++) {
        sum += num[index];
    }
    return sum;
}

int main() {
    int number = 0;
    incrementByOne(&number);

    int num[] = {1, 2, 3, 4, 5};
    int size = sizeof num/sizeof num[0];
    int sum = calculateSum(num, size);
    printf("%d\n", sum);
    return 0;
}*/
